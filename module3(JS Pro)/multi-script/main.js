console.log("Multi Script");

function add(num1, num2){
return num1 + num2
}

var city="Delhi"

 const promise=new Promise()

 




// Drawback

// 1. Many HTTP requests → slows page load due to network
//  and connection overhead. 



// Harder to manage script dependency / load 
// order issues (if script A depends on B but B loads after A,
// leads to errors). 


// Global namespace pollution — risk of name collisions 
// (variables/functions from different scripts 
//   overriding each other). 



// Increased latency and slower perceived performance (user waits longer before page becomes interactive). 
// Credera

